# gspot

One command that installs the house style for AI-written code: configured linters, the missing
rules, and agent instructions. This package is a launcher; the binary for your platform arrives as
an optional dependency and nothing runs at install time.

```bash
bunx gspot init
```

The manual is at <https://gspot.dev>.
