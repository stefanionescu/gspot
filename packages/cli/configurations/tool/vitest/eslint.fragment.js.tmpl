    {
        files: TESTS,
        plugins: { vitest },
        rules: {
            ...vitest.configs.recommended.rules,
            'vitest/no-focused-tests': ['error', { fixable: false }],
            'vitest/no-disabled-tests': 'error',
            'vitest/no-identical-title': 'error',
            'vitest/no-standalone-expect': 'error',
            'vitest/no-commented-out-tests': 'error',
            'vitest/expect-expect': 'error',
            'vitest/valid-describe-callback': 'error',
            'vitest/no-conditional-expect': 'error',
            'vitest/valid-expect': 'error',
            'vitest/prefer-strict-equal': 'error',
        },
    },
<% if (isAll) { for (const selected of configurationScopes('vitest')) { %>    {
        files: [<%= json(selected.path === '' ? '**/*' : `${selected.path}/**/*`) %>],
        rules: { 'gspot/tests-directory-contents': ['error', { harnessDirectory: <%= json([selected.path, selected.settings['tools.vitest.harness_directory']].filter(Boolean).join('/')) %> }] },
    },
<% } } %>
