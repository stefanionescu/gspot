    {
        files: ['**/*.{js,jsx,ts,tsx}'],
        plugins: { 'react-hooks': reactHooks, react, 'jsx-a11y': accessibility, 'react-refresh': reactRefresh },
        languageOptions: react.configs.flat.recommended.languageOptions,
        settings: { react: { version: 'detect' } },
        rules: {
            ...react.configs.flat.recommended.rules,
            ...react.configs.flat['jsx-runtime'].rules,
            // The hooks plugin ships two of these rules as warnings.
            ...errorLevels(reactHooks.configs.flat['recommended-latest'].rules),
            // The accessibility rules enforce the accessibility guide.
            ...accessibility.flatConfigs.recommended.rules,
            // A file of components keeps their state on a reload only when it exports nothing else.
            ...reactRefresh.configs.recommended.rules,
            'react/no-array-index-key': 'error',
            'react/no-danger': 'error',
            'react/no-unstable-nested-components': 'error',
            'react/jsx-no-constructed-context-values': 'error',
            'react/no-object-type-as-default-prop': 'error',
<% if (isAll) { %>            'react/self-closing-comp': 'error',
<% } %>        },
    },
    // The Testing Library rules read test files, and their debugging rule ships as a warning.
    { files: TESTS, plugins: { 'testing-library': testingLibrary }, rules: errorLevels(testingLibrary.configs['flat/react'].rules) },
