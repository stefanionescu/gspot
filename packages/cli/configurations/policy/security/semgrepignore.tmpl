node_modules/
dist/
build/
coverage/
.build/
DerivedData/
Pods/
.venv/
.env*
*.lock
bun.lock
package-lock.json
<% for (const path of scopeIgnorePatterns((settings['tools.semgrep.ignore'] ?? []).flatMap((entry) => entry.paths ?? []), scope)) { %><%= path %>
<% } %>
