<% const config = prettierConfig(targetPath); const svelte = has('svelte'); const plugins = [...(config.plugins ?? []), ...(svelte ? [`${'../'.repeat(targetPath.split('/').length - 1) || './'}.gspot/node_modules/prettier-plugin-svelte/plugin.js`] : [])]; const overrides = [...(svelte ? [{ files: '*.svelte', options: { embeddedLanguageFormatting: 'auto' } }] : []), ...(config.overrides ?? [])]; %><%= json({ ...config, ...(plugins.length === 0 ? {} : { plugins }), ...(overrides.length === 0 ? {} : { overrides }) }) %>
