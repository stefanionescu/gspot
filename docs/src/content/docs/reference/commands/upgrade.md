---
title: "gspot upgrade"
description: "Move this repository to this gspot version: report the changes, ask, re-render, baseline what arrives"
---

Move this repository to this gspot version: report the changes, ask, re-render, baseline what arrives.

```text
gspot upgrade [options]
```

## Options

| Flag             | Meaning                                             |
| ---------------- | --------------------------------------------------- |
| `--check`        | Print the report and write nothing                  |
| `--to <version>` | Move to an exact version, downward included         |
| `--yes`          | Skip the question                                   |
| `--no-install`   | Skip the install step and print the command instead |
