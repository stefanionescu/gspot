---
title: "gspot ignore"
description: "Turn a check, or one rule in it, off for some paths or everywhere"
editUrl: "https://github.com/stefanionescu/gspot/blob/main/packages/cli/src/program.ts"
---

<!-- Generated by bun docs/reference-pages.ts. -->

gspot 0.1.0 · [Source definition](https://github.com/stefanionescu/gspot/blob/main/packages/cli/src/program.ts)

Turn a check, or one rule in it, off for some paths or everywhere.

```text
gspot ignore [options] <check>
```

## Arguments

| Argument | Meaning  |
| -------- | -------- |
| `check`  | required |

## Options

| Flag                | Meaning                                                                 |
| ------------------- | ----------------------------------------------------------------------- |
| `--version`         | Print the version and nothing else                                      |
| `--json`            | Print the documented JSON object instead of text                        |
| `--quiet`           | Print failures only                                                     |
| `--verbose`         | Print every command with its arguments and every ignore with its reason |
| `--no-color`        | No color in the output                                                  |
| `-C <dir>`          | Run as if started in that directory                                     |
| `--paths <glob...>` | The paths the ignore applies to; none means the whole scope             |
| `--rule <rule>`     | One rule inside the check                                               |
| `--reason <text>`   | Optional explanation; required when require_reasons is true             |
| `--remove`          | Delete the matching entry instead                                       |
| `-h, --help`        | display help for command                                                |
