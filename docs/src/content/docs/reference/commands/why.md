---
title: "gspot why"
description: "Print which presets claim a file, the checks that run on it, and the baselines and ignores that touch it"
---

Print which presets claim a file, the checks that run on it, and the baselines and ignores that touch it.

```text
gspot why [options] <path>
```

## Arguments

| Argument | Meaning  |
| -------- | -------- |
| `path`   | required |
