#!/bin/bash
# macOS and Linux, Bash 3.2 or later. Resolve source without changing the caller directory.
set -euo pipefail
source_root="$(CDPATH='' cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd -P)"
exec bun "${source_root}/packages/cli/src/main.ts" "$@"
