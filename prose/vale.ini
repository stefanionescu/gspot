# Vale configuration for the gspot rule corpus and the docs. StylesPath is relative to this file.
StylesPath = styles
MinAlertLevel = error

[*.md]
BasedOnStyles = gspot
# Front matter and fenced code are not prose.
BlockIgnores = (?s)^---\n.*?\n---\n
TokenIgnores = (`[^`]+`), (\$\{[^}]+\}), ("[^"\n]+")
