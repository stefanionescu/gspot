# Prior Art

This document decides what gspot copies from tools people already trust, what it does
differently, and which libraries it reuses instead of writing its own. It exists so nobody shops
twice. A row here is a recorded choice; a new choice supersedes it with a new row and rationale
in the [owning contract](README.md#contract-owners).

## Tools gspot learns from

| Tool                                                                                                                               | What it does well                                                                                                                                                                                                                                                                             | What gspot copies                                                                                                                                  | What gspot does differently                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [qlty](https://github.com/qltysh/qlty)                                                                                             | The closest prior art. `qlty init` reads the repository and enables plugins; `qlty check`, `qlty fmt`, `qlty smells`; sixty-plus linter plugins with pinned versions in `.qlty/qlty.toml`; docs ship an `llms.txt` and a page on working with AI agents                                       | The command shape; the `llms.txt` file and the agents page in gspot's docs                                                                         | gspot never downloads tools; it writes real configuration at the paths editors read; it adds the slop rules, the naming engine and the agent rule files, which qlty has no equivalent of. qlty's Fair Source license (BSL 1.1) forbids competing use, so it is never a dependency |
| [Trunk Check](https://docs.trunk.io/references/cli/configuration)                                                                  | `.trunk/trunk.yaml` pins the CLI and every linter; `trunk check` defaults to files changed against upstream                                                                                                                                                                                   | One version pins everything                                                                                                                        | `check` runs the whole tree by default; `--since <ref>` is the pull-request form. Trunk is closed source and downloads its tools                                                                                                                                                  |
| [pre-commit](https://github.com/pre-commit/pre-commit.com/blob/main/sections/advanced.md) and [prek](https://github.com/j178/prek) | One config with pinned hook revisions; `run --all-files`; `autoupdate --freeze`; the [identify](https://github.com/pre-commit/identify) library tags files (`shell`, `python`, `executable`) from extension, shebang, executable bit and content, so hooks select by tag instead of extension | Preset claims may name tags as well as extensions, computed the way identify does                                                                  | gspot writes the tools' configuration and owns the rules; pre-commit only runs hooks                                                                                                                                                                                              |
| [Biome](https://biomejs.dev/guides/migrate-eslint-prettier/)                                                                       | `biome migrate eslint` reads an ESLint config through rule metadata; `biome explain <rule>` says why a rule exists and shows a snippet; the npm package is a thin launcher over per-platform `optionalDependencies`                                                                           | `explain`; the per-platform npm packages                                                                                                           | Takeover replaces rather than migrates, because the point is to adopt the shipped policy                                                                                                                                                                                          |
| [Ruff](https://docs.astral.sh/ruff/linter/)                                                                                        | `ruff rule <code> --output-format json` explains a rule; `--output-format`; `--statistics`; unsafe fixes are marked; a `help:` line under every finding                                                                                                                                       | The `help:` line under every finding; `ruff rule` as the source for `explain python/ruff <code>`                                                   | none; Ruff is a tool gspot drives                                                                                                                                                                                                                                                 |
| [uv](https://docs.astral.sh/uv/) and [mise](https://mise.jdx.dev/dev-tools/backends/ubi.html)                                      | Single binary; `doctor`; `mise.toml` with `conf.d/` merging; `github:` installs any GitHub-release binary; settings validated through a published JSON schema                                                                                                                                 | The runner integration, the pin, `doctor`, and a published `gspot.schema.json`                                                                     | none                                                                                                                                                                                                                                                                              |
| [gh](https://cli.github.com/) and [cargo](https://doc.rust-lang.org/cargo/)                                                        | `--json` everywhere; stable exit codes; `cargo --message-format json`                                                                                                                                                                                                                         | `--json` is global                                                                                                                                 | no `--jq`; a person has `jq`                                                                                                                                                                                                                                                      |
| [lint-staged](https://github.com/lint-staged/lint-staged)                                                                          | Hides unstaged changes behind a stash and restores them; `--fail-on-changes`; documents every failure mode of the stash                                                                                                                                                                       | The honest line about working-tree content; fixes in a hook never stage themselves                                                                 | gspot reads working-tree content and says so instead of stashing ([10-hooks-ci-runners.md](10-hooks-ci-runners.md))                                                                                                                                                               |
| [treefmt](https://github.com/numtide/treefmt)                                                                                      | One `treefmt.toml`; one formatter per file; parallel; a cache on file metadata                                                                                                                                                                                                                | Confirms the tool runner in [05-engines.md](05-engines.md)                                                                                         | gspot's cache key is the content hash plus tool version plus configuration hash                                                                                                                                                                                                   |
| [projen](https://github.com/projen/projen/blob/main/ARCHITECTURE.md)                                                               | Generated files carry a magic header, are read-only on disk, and an anti-tamper check runs in CI; synthesis finds generated files by their header, not a stored list                                                                                                                          | `integrity/generated-drift` finds generated files by their mark; generated files are written read-only where the platform allows                   | gspot generates tool configuration, not the project                                                                                                                                                                                                                               |
| [MegaLinter](https://megalinter.io/latest/descriptors/yaml_yamllint/)                                                              | One YAML descriptor per linter: install, command line, config file, output format, file types; over a hundred of them                                                                                                                                                                         | The descriptors are the reference when a preset manifest is written: every tool gspot ships has one to read the command line and output shape from | gspot does not run in Docker and ships a curated set, not every linter                                                                                                                                                                                                            |
| [clig.dev](https://clig.dev/)                                                                                                      | The checklist for human-first command lines                                                                                                                                                                                                                                                   | The Conventions section of [02-cli.md](02-cli.md)                                                                                                  | none                                                                                                                                                                                                                                                                              |

## Libraries gspot reuses

Each row names a job the design once implied gspot writes itself, and the library that does
it. [12-repository-layout.md](12-repository-layout.md) lists the same libraries by job.

| Job                                       | Library                                                                                                                                                                                                  | What it replaces                                                                                        |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| npm distribution                          | per-platform packages as `optionalDependencies` with `os` and `cpu` fields and a thin `bin` launcher, the pattern of `@biomejs/biome` and [`@ast-grep/cli`](https://www.npmjs.com/package/@ast-grep/cli) | a wrapper that downloads the binary at install, which fails behind proxies and under `--ignore-scripts` |
| Writing `gspot.toml` with comments intact | [`@decimalturn/toml-patch`](https://github.com/DecimalTurn/toml-patch): TOML 1.1, `patch()` and `TomlDocument`, comments travel with the entry they belong to                                            | a line-based appender                                                                                   |
| Package manager detection                 | [`nypm`](https://github.com/unjs/nypm) (`detectPackageManager`)                                                                                                                                          | Reads the package manager from the repository.                                                          |
| `.gitignore` semantics without git        | [`ignore`](https://www.npmjs.com/package/ignore) with native directory traversal                                                                                                                                  | A hand-written matcher.                                                                                 |
| Naming a language gspot has no preset for | [`linguist-languages`](https://www.npmjs.com/package/linguist-languages), GitHub Linguist's data as a package                                                                                            | a hand-kept extension table                                                                             |
| SARIF                                     | [`node-sarif-builder`](https://github.com/nvuillam/node-sarif-builder)                                                                                                                                   | a hand-written writer                                                                                   |
| Shell completions                         | [`@bomb.sh/tab`](https://github.com/bombshell-dev/tab) with its commander adapter; used by Wrangler, Nuxt, Astro, and Vitest                                                                             | a `completion` command written by hand, which is why it sat in v1.1                                     |
| JSON schema for `gspot.toml`              | zod v4 [`z.toJSONSchema`](https://zod.dev/json-schema); `zod-to-json-schema` is unmaintained since November 2025                                                                                         | nothing; adds editor validation through SchemaStore                                                     |
| Preset templates                          | [`eta`](https://eta.js.org/)                                                                                                                                                                             | a placeholder renderer of gspot's own                                                                   |
| Command parsing and help                  | [commander](https://github.com/tj/commander.js)                                                                                                                                                          | the most used option; its help text is the command reference                                            |
| Questions at `init`                       | [`@clack/prompts`](https://github.com/bombshell-dev/clack)                                                                                                                                               | any other prompt library                                                                                |
| Parsing for the engines                   | [`web-tree-sitter`](https://github.com/tree-sitter/tree-sitter), [`libpg-query`](https://github.com/pganalyze/libpg_query) WASM, the [ast-grep CLI](https://ast-grep.github.io/)                         | a parser of any kind                                                                                    |
| Identifier splitting                      | [`scule`](https://github.com/unjs/scule)                                                                                                                                                                 | the naming engine's case splitter                                                                       |
| Workspace discovery                       | [`@manypkg/tools`](https://www.npmjs.com/package/@manypkg/tools)                                                                                                                                         | reading `workspaces`, `pnpm-workspace.yaml`, Lerna, and Rush by hand                                    |
| JSON with comments, YAML, `package.json`  | [`jsonc-parser`](https://github.com/microsoft/node-jsonc-parser), [`yaml`](https://github.com/eemeli/yaml)                 | writers that lose comments or indentation                                                               |
| License expressions                       | [`spdx-expression-parse`](https://www.npmjs.com/package/spdx-expression-parse), [`spdx-satisfies`](https://www.npmjs.com/package/spdx-satisfies)                                                         | string comparison of license names                                                                      |
| Markdown structure                        | `mdast-util-from-markdown`, `mdast-util-to-string`, and `unist-util-visit`                                                                                                                               | Syntax trees for headings, examples, README structure, and path exclusions.                             |
| CSS structure and selectors               | `postcss`, `postcss-scss`, and `postcss-selector-parser`                                                                                                                                                 | Stylesheet rules and decoded class names.                                                               |
| Diffs                                     | [`diff`](https://www.npmjs.com/package/diff)                                                                                                                                                             | a diff printer                                                                                          |
| Concurrency                               | [`p-limit`](https://www.npmjs.com/package/p-limit)                                                                                                                                                       | a queue                                                                                                 |
| Messages on stderr                        | [`consola`](https://github.com/unjs/consola)                                                                                                                                                             | a logger with levels and TTY detection                                                                  |
| Process execution                         | [Execa](https://github.com/sindresorhus/execa)                                                                                                                                                           | Captured streams, deadlines, cancellation, and platform command shims.                                  |
| Release                                   | [`actions/attest`](https://github.com/actions/attest-build-provenance), `gh release create`, [`verdaccio`](https://github.com/verdaccio/verdaccio) for the publish test                        | release scripts                                                                                         |
| Docs site                                 | [Starlight](https://starlight.astro.build/) with [`starlight-llms-txt`](https://github.com/delucis/starlight-llms-txt)                                                                                   | a site generator and an `llms.txt` writer                                                               |

## Infrastructure reuse candidates

This review records adopted libraries and candidates for remaining infrastructure.
[Library selection](12-repository-layout.md#libraries) defines the decision order. Prove each
replacement at the boundary it owns, then remove the old implementation and update its callers.
Keep the result in the existing owning acceptance item, not a separate audit framework.

| Responsibility               | Candidate                                                                                        | Required evidence before adoption                                                                                                                                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Temporary test directories   | [testdirs](https://github.com/luxass/testdirs)                                                   | Adopted as a direct development dependency used by product tests. Acquire the disposable directory before filling it. Do not maintain tests of its own API.                                                                       |
| Temporary directory disposal | [Node filesystem APIs](https://nodejs.org/api/fs.html#fspromisesmkdtempdisposableprefix-options) | Prefer native operations where supported. `mkdtempDisposable` requires Node 24.4.0 or later, above the declared Node floor; the local Bun 1.3.11 probe exposes no such function. Do not assume Bun implements a Node API.         |
| Process lifecycle            | [Execa](https://github.com/sindresorhus/execa)                                                   | Adopted for the shared process boundary after Bun and compiled-consumer execution. Preserve the process-failure regressions; Windows execution, process-tree termination, and command-session cancellation remain required gates. |
| Atomic file replacement      | [write-file-atomic](https://github.com/npm/write-file-atomic)                                    | Replacement and cleanup failures, bytes and modes, concurrency, and supported-platform behavior. A single-file atomic writer does not establish path confinement, multi-file recovery, or ownership.                              |
| Writer serialization         | [proper-lockfile](https://github.com/moxystudio/node-proper-lockfile)                            | Contention, interrupted writers, stale locks, lock compromise, and cleanup. Verify its behavior against the lifecycle transaction before adopting it.                                                                             |
| Git worktrees and revisions  | The installed Git executable through the shared runner                                           | Exact staged and pushed objects, checked exits, worktree preservation, and cleanup. Do not add a Git wrapper that only renames argument arrays.                                                                                   |
| Installed-package acceptance | The existing Verdaccio dependency and npm commands                                               | Own the server process, port, registry routing, publication, consumer isolation, and shutdown. Do not implement a registry or fake package installation.                                                                          |

Tests import `testdirs` directly. No private workspace package or forwarding helper owns that
API. Use real files to verify product subprocess execution and preservation of user files.
Do not add dependency API tests or tests of test helpers. Evaluate dependencies before adoption;
keep regression tests only for behavior owned by gspot.

The accepted package has an MIT license. Its Zod dependency is already used by the repository.
The exact version belongs to `package.json` and the lockfile. Its API does not provide the lifecycle path-confinement or
recovery contract required for mutations of a developer repository. Do not use disposable test
directories as the production mutation boundary.

Execa 9.6.1 has an MIT license and supports the declared Node floor. It owns process capture,
timeout and cancellation handling, and command shims; its transitive dependencies include
cross-spawn. The CLI removes its direct cross-spawn dependency and both platform backends.
A capture error must explicitly terminate the Execa subprocess: the library otherwise waits for
exit before returning that error. The regression injects a stream error and verifies prompt
termination, preserved diagnostics, and no timeout or user-cancellation classification.
The library does not own gspot tool selection, version policy, domain preparation, or reports.

## Considered and not adopted

| Candidate                                                            | For                                                        | Why not                                                                                                                                                                                                          |
| -------------------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [dependency-cruiser](https://github.com/sverweij/dependency-cruiser) | JavaScript and TypeScript import rules, cycles, boundaries | Maintained and thorough, but it runs as its own command with no editor feedback; the ESLint plugin was chosen so the editor shows the rule. It is the fallback if `eslint-plugin-boundaries` proves insufficient |
| [tach](https://github.com/tach-org/tach)                             | Python module boundaries                                   | import-linter already covers the contracts and lives in `pyproject.toml`; two tools for one job duplicates responsibility                                                                                        |
| [ls-lint](https://ls-lint.org/)                                      | File and directory case rules                              | The naming engine already extracts every name for banned-term matching; a second tool would do half the check                                                                                                    |
| [Stricli](https://bloomberg.github.io/stricli/)                      | Command framework                                          | Typed, zero dependencies, autocomplete built in, but bash-only completions and a small community. commander stays                                                                                                |
| [Clipanion](https://github.com/arcanis/clipanion)                    | Command framework                                          | v4 has sat at release candidate since 2024                                                                                                                                                                       |
| [oclif](https://oclif.io/)                                           | Command framework with installers and an update command    | About 50 MB of dependencies, Node-only, unproven under `bun build --compile`                                                                                                                                     |
| [qlty](https://github.com/qltysh/qlty) as an engine                  | Running the linters                                        | Fair Source license; downloads tools itself                                                                                                                                                                      |
| [MegaLinter](https://megalinter.io/) as an engine                    | Running the linters                                        | Docker-only; runs everything rather than a curated set                                                                                                                                                           |
| [linguist-js](https://github.com/Nixinova/LinguistJS)                | Language detection over the tree                           | Fetches Linguist data at run time; gspot uses the `linguist-languages` data package offline instead                                                                                                              |

The current lifecycle boundary uses standard filesystem APIs in
`packages/cli/src/lifecycle/confined.ts`. It validates repository-relative paths and existing
parents, checks expected bytes and modes before replacement, and supports journaled recovery.
It does not use `__openat` or provide handle-relative protection against hostile concurrent
directory replacement. That race is outside the contract. Native platform evidence and
remaining ownership work belong in [remaining work](22-remaining.md).

## Nx and Turborepo

Read-only inspection used the adjacent checkouts at Nx `59b35fb` (revision prefix
`59b35fba73`) and Turborepo `1dead3c` (full revision prefix `1dead3cc9d`). These are
inspected snapshots, not a claim about their latest upstream state.

Nx keeps schemas with their implementing features. For example, compare
`../nx/packages/jest/src/generators/configuration/schema.json` with its generator implementation.
The documentation consumer is visible in `../nx/astro-docs/src/content.config.ts` and
`../nx/astro-docs/src/plugins/plugin.loader.ts`: schema-derived content is rendered into store
entries. `../nx/astro-docs/src/plugins/nx-reference-packages.loader.ts` handles reference packages.
Adopt feature ownership and content-store rendering. Do not copy its network statistics,
package taxonomy, or error-handling choices as gspot requirements. Gspot rejects duplicate
identities and invalid required definitions before publication.

Turborepo separates its launcher in `../turborepo/packages/turbo/package.json` from
`../turborepo/packages/eslint-plugin-turbo/package.json`. Its release platform definition is
`../turborepo/packages/turbo-releaser/src/config.ts`; `native.ts` consumes platform rows to stage
npm metadata, binaries, and licenses. The launcher consumes installed native packages. Schema
publication has a release consumer in `../turborepo/packages/turbo-releaser/src/stage.ts`.
Adopt these boundaries and one platform definition feeding packaging. Keep gspot's own seven
supported targets and its independent ESLint exports, rather than copying Turborepo's matrix.

Both projects distinguish repository maintenance tooling from shipped features. For gspot,
keep Bun and native mise tasks. Keep runtime policy, configuration schemas, and embedded WASM
with their consumers; stage generated copies in ignored build output. Package licenses and
binary notices serve different consumers. Notice assembly stays with packaging, and plugin
dependencies remain external. No Nx or Turborepo dependency, private package, or new task
framework follows from this comparison.

Reuse Nx feature ownership and content loaders, and Turborepo launcher, plugin, and packaging boundaries. Do not copy their migration systems or introduce their task frameworks.
