extends: existence
message: "A file path in prose goes stale: '%s'. Name the concept, or put the path in code formatting only where the reader must open it."
level: error
scope: paragraph
nonword: true
tokens:
    - "(?<!`)\\b[a-z0-9_-]+/[a-z0-9_./-]+\\.(?:ts|js|py|swift|sh|sql|md|json|toml|yml|yaml)\\b(?!`)"
