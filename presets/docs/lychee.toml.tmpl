offline = true
include_fragments = "full"
no_progress = true
exclude = <%= json((settings['tools.lychee.exclude'] ?? []).flatMap((entry) => typeof entry === 'string' ? [entry] : (entry.patterns ?? []))) %>
