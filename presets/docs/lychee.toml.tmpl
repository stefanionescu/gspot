<%~ toml({
    offline: true,
    include_fragments: 'full',
    no_progress: true,
    exclude: (settings['tools.lychee.exclude'] ?? []).flatMap((entry) => typeof entry === 'string' ? [entry] : (entry.patterns ?? [])),
}).trimEnd() %>
