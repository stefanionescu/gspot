id: structure/mutable-assignments
language: bash
message: This function reassigns more than the ceiling allows.

rule:
  kind: variable_assignment
  inside:
    stopBy: end
    kind: function_definition
  not:
    inside:
      stopBy: neighbor
      kind: declaration_command
