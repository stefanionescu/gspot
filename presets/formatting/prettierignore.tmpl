# Editors read this file; the gate hands Prettier an explicit file list and never reads it.
node_modules/
dist/
build/
coverage/
.gspot/cache/
.gspot/report.json
.gspot/report.sarif
*.lock
bun.lockb
package-lock.json
pnpm-lock.yaml
yarn.lock
<% for (const file of policy.declares.flatMap((entry) => entry.paths)) { %><%= file %>
<% } %>
