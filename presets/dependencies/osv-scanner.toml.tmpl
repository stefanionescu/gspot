<% const entries = settings['tools.osv.ignore'] ?? []; %><%~ toml(entries.length === 0 ? {} : {
    IgnoredVulns: entries.map((entry) => ({
        id: entry.id,
        ...(entry.review_by ? { ignoreUntil: new tomlDate(entry.review_by) } : {}),
        reason: entry.reason,
    })),
}).trimEnd() %>
