    {
        files: ['**/*.{js,jsx,ts,tsx}'],
        plugins: { 'react-hooks': reactHooks, react },
        settings: { react: { version: installedReact() } },
        rules: {
            // The plugin ships this set as a list of one block, and its warnings are errors here: the gate allows no warning.
            ...Object.fromEntries(Object.keys(reactHooks.configs['recommended-latest'][0].rules).map((rule) => [rule, 'error'])),
            'react/jsx-key': 'error',
            'react/jsx-no-target-blank': 'error',
            'react/no-array-index-key': 'error',
            'react/no-danger': 'error',
            'react/no-unstable-nested-components': 'error',
            'react/jsx-no-constructed-context-values': 'error',
            'react/no-object-type-as-default-prop': 'error',
            'react/self-closing-comp': 'error',
        },
    },
